# @leavepulse/sdk

Generated TypeScript SDK package for LeavePulse platform contracts.

The source in this repository is generated from the private `leavepulse-sdk`
generator and checked in so consumers can install it as a normal package.

```ts
import { LeavePulse } from "@leavepulse/sdk"
```

Do not edit generated files manually. Regenerate them from `leavepulse-sdk`.
